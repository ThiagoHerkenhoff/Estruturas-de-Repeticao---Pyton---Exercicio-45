# 45 - Faça um algoritmo que converta uma velocidade expressa em km/h para m/s e vice versa.
# Você deve criar um menu com as duas opções de conversão e com uma opção para finalizar o programa.
# O usuário poderá fazer quantas conversões desejar, sendo que o programa só será finalizado quando
# a opção de finalizar for escolida

print('1 - Conversão de Km/h para m/s')
print('2 - Conversão de m/s para Km/h')
print('3 - finalizar')

while True:

    opcao = int(input('Informe o número da opção desejada: '))

    if opcao == 1:
        velocidade = float(input('Informe a velocidade: '))
        ms = velocidade / 3.6
        print(f'A velocidade informada {velocidade}Km/m é de {ms}m/s')

    elif opcao == 2:
        velocidade = float(input('Informe a velocidade: '))
        kmh = velocidade * 3.6
        print(f'A velocidade informada {velocidade}m/s é de {kmh}Km/h')

    else:
        break
